language: php

php:
  - 5.4
  - 5.5
  - 5.6
  - 7.0
  - 7.1
  - 7.2
  - hhvm
  
before_script:
  - git update-index --add --chmod=+x build.sh
  
script:
  - ./build.sh

before_install:
  - travis_retry composer self-update

install:
  - composer install
